// Re-export of the [OtpChannel] enum to keep the legacy import path stable.
//
// The real definition lives in `enums.dart` so all enums share a single
// source of truth.
export 'package:origin_mobile/data/models/enums.dart' show OtpChannel;
